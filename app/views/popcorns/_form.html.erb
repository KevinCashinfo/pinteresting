<%= form_for(@popcorn) do |f| %>
  <% if @popcorn.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@popcorn.errors.count, "error") %> prohibited this popcorn from being saved:</h2>

      <ul>
      <% @popcorn.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
